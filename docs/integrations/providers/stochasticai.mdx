# 随机AI

本页面介绍如何在LangChain中使用随机AI生态系统。
它分为两个部分：安装和设置，以及对特定随机AI包装器的引用。

## 安装和设置
- 使用 `pip install stochasticx` 安装
- 获取一个随机AI API密钥并将其设置为环境变量 (`STOCHASTICAI_API_KEY`)

## 包装器

### 大型语言模型

存在一个随机AI大型语言模型包装器，您可以通过以下方式访问
```python
from langchain_community.llms import StochasticAI
```