# Lantern

本页面介绍如何在LangChain中使用[Lantern](https://github.com/lanterndata/lantern)
它分为两个部分：设置和对特定Lantern包装器的引用。

## 设置
1. 第一步是创建一个安装了`lantern`扩展的数据库。

请按照[Lantern安装指南](https://github.com/lanterndata/lantern#-quick-install)中的步骤安装数据库和扩展。使用docker镜像是最简单的入门方式。

## 包装器

### 向量存储

存在一个围绕Postgres向量数据库的包装器，允许您将其用作向量存储，
无论是用于语义搜索还是示例选择。

要导入这个向量存储：
```python
from langchain_community.vectorstores import Lantern
```

### 使用

有关 Lantern Wrapper 的更详细说明，请参见 [此笔记本](/docs/integrations/vectorstores/lantern)
