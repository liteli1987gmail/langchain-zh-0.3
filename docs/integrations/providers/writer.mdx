# Writer

本页面介绍如何在LangChain中使用Writer生态系统。
它分为两个部分：安装和设置，以及对特定Writer包装器的引用。

## 安装和设置
- 获取一个Writer API密钥并将其设置为环境变量（`WRITER_API_KEY`）

## 包装器

### LLM

存在一个Writer LLM包装器，您可以通过它访问
```python
from langchain_community.llms import Writer
```