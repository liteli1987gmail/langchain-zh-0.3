# MariTalk

>[MariTalk](https://www.maritaca.ai/en) 是一个基于大型语言模型的聊天机器人，旨在满足巴西的需求。

## 安装和设置

您需要获取 MariTalk API 密钥。

您还需要安装 `httpx` Python 包。

```bash
pip install httpx
```

## 聊天模型

查看[使用示例](/docs/integrations/chat/maritalk)。

```python
from langchain_community.chat_models import ChatMaritalk
```
