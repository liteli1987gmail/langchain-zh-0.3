# Notion 数据库

> [Notion](https://www.notion.so/) 是一个支持修改过的Markdown的协作平台，集成了看板
> 板、任务、维基和数据库。它是一个用于笔记、知识和数据管理的全能工作区，
> 以及项目和任务管理。

## 安装和设置

所有说明都在下面的示例中。

## 文档加载器

我们有两种不同的加载器：`NotionDirectoryLoader` 和 `NotionDBLoader`。

请查看 [使用示例](/docs/integrations/document_loaders/notion)。


```python
from langchain_community.document_loaders import NotionDirectoryLoader, NotionDBLoader
```
